function name_count(str){
    var lng = str.length;
    $('.namecount').html(lng);
}

function title_count(str){
    var lng = str.length;
    $('.charcount').html(lng);
}

function cmt_count(str){
    var lng = str.length;
    $('.cmtcount').html(lng);
}