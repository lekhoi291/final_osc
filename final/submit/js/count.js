function title_count(str){
    var lng = str.length;
    $('.charcount').html(lng);
}

function cmt_count(str){
    var lng = str.length;
    $('.cmtcount').html(lng);
}

function price_count(str){
    var lng = str.length;
    $('.price_count').html(lng);
}